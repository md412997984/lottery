算法原理
该抽奖算法基于双色球开奖号码，使用哈希函数计算结果值，并根据结果值选择离其最近的指定个数的用户ID。算法的关键点在于使用哈希函数将双色球号码映射到一个结果值，并利用结果值与用户ID之间的距离进行排序和选择。

算法逻辑
CalculateResult函数：该函数接受一个双色球开奖号码的红球列表作为输入，使用哈希函数计算结果值，并返回结果值作为抽奖结果。

SelectClosestValues 函数：该函数接受结果值、用户池中的ID列表、需要选择的ID个数以及选择大于还是小于结果值的标志作为输入。它首先根据用户ID与结果值的绝对值距离进行排序，然后选择离结果值最近的指定个数的ID。

注意事项
请确保提供的双色球开奖号码与算法一致，以确保结果的可预测性。
用户池中的ID列表应包含所有参与抽奖的用户ID。
选择大于结果值的ID时，结果值应较小；选择小于结果值的ID时，结果值应较大。
在选择中奖用户时，可能存在用户ID不足的情况，请确保用户池中的ID数量满足需求。
可根据实际情况对算法进行调整和扩展，以满足具体的抽奖规则和要求。
以上是抽奖算法的原理、逻辑和使用说明。根据实际需求和规则，您可以进行相应的修改和定制。